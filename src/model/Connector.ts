/**
 * !WORK IN PROGRESS
 * At the moment this is simply a tagging interface, but it should become an abstraction of the Connectors at some point, to make them interchangeable
 */
export interface Connector{

}
